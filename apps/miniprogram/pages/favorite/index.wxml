<view class="page">
  <view class="page-title">收藏</view>
  <view class="page-subtitle">{{status}}</view>
  <view wx:for="{{favorites}}" wx:key="id" class="empty-panel">
    <view>{{item.targetType}} · {{item.targetId}}</view>
  </view>
</view>
